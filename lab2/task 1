numbers = input("Введите числа в одну строку ").split()
seen_numbers = set()

for num in numbers:
    if num in seen_numbers:
        print("yes")
    else:
        print("no")
        seen_numbers.add(num)
